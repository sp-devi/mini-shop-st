import * as React from 'react'

type CartProps = {
    itemInCartAmount: number;
}

type CartState = {
    itemInCartAmount: number;
}

export const AddToCart = (itemInCartAmount: CartProps) => {
    return(
        <div>
        </div>
    )
}